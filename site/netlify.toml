# Statische Multi-Page-Site – keine SPA-Redirects (sonst brechen Unterseiten)
[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"
